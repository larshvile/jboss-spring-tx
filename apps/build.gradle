subprojects {
    applyHulteScript "container/spring-deployment"
    applyHulteScript "container/provided-spring"
    // applyHulteScript "provided-jpa"
    // applyHulteScript "provided-jms" spring-jms / activemq stuff?

    defaultTasks 'clean', 'uploadArchives'

    group = 'net.hulte.apps'
    version = 'CURRENT-SNAPSHOT'
}

