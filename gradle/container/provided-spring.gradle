/*
 * Creates a dependency to the Spring framework. Should emulate what's actually deployed to JBoss,
 * see 'frameworks/spring/spring-jboss-module'.
 */
applyHulteScript "versions"
applyHulteScript "container/jboss"

// TODO if 'war' use the 'providedCompile' configuration, for regular java use the custom 'provided'??

dependencies {
    compile 'net.hulte.spring:spring-dependencies:CURRENT-SNAPSHOT'
}
// TODO testing dependencies

// jbossDependencies << 'org.springframework.spring'
jbossDependencies << 'org.jboss.snowdrop' // TODO correct?, snowdrop seems to export spring .. but does it export everything?? org.*??

