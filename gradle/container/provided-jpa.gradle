/*
 * Creates a dependency to JPA.
 */
applyHulteScript "versions"
applyHulteScript "container/jboss"

dependencies {
    compile 'org.hibernate:hibernate-entitymanager:' + jbossHibernateVersion // TODO compile is wrong here as well, see provided-spring
}

jbossDependencies << 'org.hibernate'

